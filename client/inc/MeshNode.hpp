#ifndef MESHNODE_HPP
#define MESHNODE_HPP

#include <cstddef>

/**
 * \brief   A "POD" struct to keep x,y coordinates of a node in the mesh
 */
struct MeshNode {
  double x;
  double y;
};


#endif /* MESHNODE_HPP */
